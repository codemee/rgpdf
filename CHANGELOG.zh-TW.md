# 變更紀錄

繁體中文 · [English](CHANGELOG.md)

所有重要變更都記錄於此。不可變的發布版本使用 `vX.Y.Z`；可移動的 `latest` 標籤指向最新發布版本。

## 0.0.5 — 2026-08-17

- 新增自含式 macOS arm64 App bundle、Retina `.icns` 圖示與一鍵建置腳本。
- 新增 macOS App 啟動、bundle metadata、架構與 ad-hoc 簽章驗證。
- 將發布流程改為由 `vX.Y.Z` tag 自動觸發，平行測試並建置 Windows x86-64 與 macOS arm64。
- 自動建立同一個 GitHub Release、版本化下載檔、SHA-256 校驗檔及 PyPI 發布。
- 新增經 SHA-256 驗證的 PyMuPDF、MuPDF、PySide6、Shiboken6 與 Qt 對應原始碼封存。

## 0.0.4 — 2026-08-17

- 將 rgpdf 以 GNU AGPLv3 授權，並新增完整第三方聲明與發布合規指引。
- 新增程式內「關於」對話框，提供精確版本的原始碼與授權連結。
- 新增可重現的 PyInstaller 建置，產生自含式 Windows x86-64 單一執行檔。
- 新增自動產生的多尺寸 Windows 圖示、執行檔版本資訊與封裝資源檢查。
- 新增 GitHub Actions Windows 建置與 GUI 啟動 smoke test。
- 新增未簽章、含版本編號的 GitHub Release 下載檔與 SHA-256 驗證檔。

## 0.0.3 — 2026-08-15

- 將工具列圖示統一為 `20×20 px`，按鈕為 `34×34 px`，內距為 `4 px`。
- 移除工具列按鈕外框，包含啟用狀態。
- 將工具列按鈕間距固定為 `4 px`，確保跨平台一致。

## 0.0.2 — 2026-08-15

- 將搜尋表單標籤與輸入元件垂直置中對齊。
- 新增可互動且支援雙語的搜尋樣式說明，包含範例與可點擊的規則表達式參考連結。
- 在應用程式視窗標題顯示套件版本。
- 新增透過 GitHub Trusted Publishing 安全自動發布至 PyPI 的流程。

## 0.0.1 — 2026-08-15

- 首次公開發布。
- 新增跨平台、多 PDF 的純文字與規則表達式搜尋。
- 新增逐筆漸進結果與不阻塞介面的 PDF 預覽。
- 新增精確逐字元反白與可自訂螢光筆顏色。
- 新增英文／繁體中文介面與系統／淺色／深色主題。
- 新增搜尋、外觀與視窗布局設定保存。
